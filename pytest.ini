[pytest]
# Run tests from the tests/ directory.
testpaths = tests

# Show each test name as it runs (not just a dot).
# Remove -v here and pass it on the command line if you prefer quiet output.
addopts = -v

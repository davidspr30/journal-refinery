# Copy this file to .env if you want to override any defaults.
# The .env file is never committed to version control.

# Optional: override the database path (default: data/journal.db)
# DATABASE_PATH=/path/to/your/journal.db
